import React from 'react';
import './styles.css';

const toolbar = (props) => (
    <header className="Header">
        <div className="SiteName">
            <p>Project Manager</p>
        </div>
    </header>
);

export default toolbar;